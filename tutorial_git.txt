1.- Crear la carpeta del repositorio
2.- CD a la carpeta
3.- Inicializar el repositorio
        @git init
4.- Agregar archivos al repositorio
        @git add .
5.- Validar el estatus del repositorio (opcional)
        @git status
6.- Autentificar el usuario
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
6.- Creacion de un commit
        @git commit -m'texto del commit'
7.- Creación del repocitorio en githube
8.- agregar el origen del repositorio
        @git remote add origin https://github.com/munmmra/tutorial_git.git
9.- seleccionamos la rama objetivo
        @git branch -M main seleccionamos 
10.- cargamos la informacion al repositorio
        @git push -u origin main

// para realizar procimas actualizaciones
1.- grardamos en el reposritorio local los cambios
        @git add .
2.- generamos un punto de carga
        @@git commit -m'texto del commit'
3.- seleccionamos la rama objetivo
        @git branch -M main seleccionamos 
4.- cargamos la informacion al repositorio
        @git push -u origin main


